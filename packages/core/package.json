{
  "name": "@bookends/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic shared by the API, the seed, and (later) the web client. Zero I/O.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./password/policy": {
      "types": "./dist/password/policy.d.ts",
      "default": "./dist/password/policy.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2"
  }
}
